# Archive of Historical Migrations

These files are the historical ad-hoc migrations. They are kept here for historical reference only.
The live database state is now represented by `00_*.sql` through `19_*.sql` in the parent `supabase` directory, which are regenerated directly from Supabase Studio snapshots.
